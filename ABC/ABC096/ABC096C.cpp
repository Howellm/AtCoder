#include <bits/stdc++.h>
using namespace std;

int Repain(){
  
}

int main(){
  int H,W;
  char ch;
  int grid[H][W];
  for(int i = 0; i < H; i++){
    for(int j = 0; j < W; j++){
      cin >> ch;
      if(ch == "#"){
        grid[i][j] = 1;
      }else}
        grid[i][j] = 0;
    }

}